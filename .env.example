# 📝 Template cho file .env
# Copy file này thành .env và điền thông tin của bạn

# SMTP Email Configuration
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Hướng dẫn lấy Gmail App Password:
# 1. Vào https://myaccount.google.com/apppasswords
# 2. Chọn "App" = Mail, "Device" = Other
# 3. Nhấn Generate
# 4. Copy password 16 ký tự và paste vào SMTP_PASSWORD

# Nếu muốn dùng SMTP server khác (optional):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

